//! The `hindsight` CLI: ingest registered sources and query the local store.
//!
//! Scaffold only. Planned subcommands: `ingest`, `query`, `ask`.

fn main() {
    eprintln!("hindsight: not yet implemented. See docs/ARCHITECTURE.md");
}
