[package]
name = "hindsight-server"
description = "Long-running Hindsight service: HTTP/SSE MCP transport, WebSocket live tail, and query API."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hindsight-core = { path = "../hindsight-core" }
