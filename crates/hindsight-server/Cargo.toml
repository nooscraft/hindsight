[package]
name = "hindsight-server"
description = "Long-running Hindsight service: Portal, live tail, HTTP MCP transport, and query API."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hindsight-core = { path = "../hindsight-core" }
