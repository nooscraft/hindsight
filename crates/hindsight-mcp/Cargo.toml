[package]
name = "hindsight-mcp"
description = "MCP server exposing Hindsight's read-only tools to AI agents."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hindsight-core = { path = "../hindsight-core" }
