//! Hindsight MCP server: the agent-facing surface.
//!
//! Exposes read-only tools (`list_sources`, `describe`, `query`, `search`, ...)
//! so any AI agent can discover and query local logs. Scaffold only.

fn main() {
    eprintln!("hindsight-mcp: not yet implemented. See docs/ARCHITECTURE.md");
}
